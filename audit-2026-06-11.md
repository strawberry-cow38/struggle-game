# struggle-game audit — 2026-06-11

54 agents, 6 dimensions, 48 raw findings -> 39 confirmed / 9 refuted (adversarial verify).

## HIGH

### [threading] LightAt() reads lamp/roof/sun arrays from render thread without lock
`Sim/SimRuntime.cs:6866`

SimHost.LightAt() (called from HudOverlay._Process on the Godot main thread) delegates directly to SimRuntime.LightAt() which reads _lampR[idx], _lampG[idx], _lampB[idx], _roofTiles[idx], and _lastSunR/G/B without holding _mapLock. The sim thread writes all of these arrays inside RecomputeLampLight() and EnsureRoofArrays(), both of which DO NOT hold _mapLock. EnsureRoofArrays can also replace the entire _lampR/G/B array reference (_lampR = new byte[n]), so the render thread can read from a stale reference after the sim thread replaced it. CopyLightRgbForRender() correctly acquires _mapLock; LightAt() does not, leaving it with a different synchronization contract than all the other render-path methods.

> verify: The claim is confirmed by the code. SimHost runs the sim loop on a dedicated background thread (SimHost.cs line 21/55: `new Thread(Loop)`). LightAt() at SimRuntime.cs:6866 reads _lampR[idx], _lampG[idx], _lampB[idx], _roofTiles[idx], and _lastSunR/G/B with no lock. The sim thread calls RecomputeLampLight() (line 6711) which writes into these same arrays, also without holding _mapLock. EnsureRoofAr

### [threading] IsPlayerWall() reads _playerWalls List<TilePos> from render thread without lock
`Game/SimHost.cs:248`

SimHost.IsPlayerWall() is called from WallInfoPanel and Bootstrap.cs on the Godot main thread. It calls _sim.PlayerWalls.Contains(tile), reading the live List<TilePos> _playerWalls. The sim thread mutates _playerWalls via Add/Remove inside lock(_mapLock) during wall build/decon. IsPlayerWall() takes neither _swapLock nor _mapLock, so it races with those mutations. A concurrent structural modification to List<TilePos> (Add reallocates the backing array; Remove shifts elements) during Contains() is undefined behaviour and can corrupt the enumerator state.

> verify: The race is real and unguarded. SimRuntime._playerWalls is a List&lt;TilePos&gt; (SimRuntime.cs:170) mutated by the sim thread under _mapLock (Add at lines 2693, 2742; Remove at lines 2094, 3024). SimRuntime.PlayerWalls (line 3338) returns _playerWalls cast to IReadOnlyList&lt;TilePos&gt; — the same live list object, not a snapshot or copy. SimHost.IsPlayerWall() (SimHost.cs:248) calls _sim.Player

### [threading] Double-buffer snapshot slots can be overwritten while render thread holds a reference
`Sim/SimRuntime.cs:1228`

BuildSnapshot alternates between _snapSlotA and _snapSlotB (_useSlotA = !_useSlotA). The invariant is that the render thread reads the previously published slot while the sim writes the other. But SnapshotList<T> deliberately reuses the same backing T[] arrays across ticks (pooled for GC). If the sim runs two ticks faster than the render thread polls _latest, on tick N+2 it will write into the same slot the render thread is currently reading (slot A → B → A). The SnapshotList enumerator holds a direct reference into the pooled T[] array; a concurrent write from the sim thread produces torn reads of pawn positions, health, etc. This is worsened at high tick rates (6x speed uses 6× normal Hz).

> verify: The claim is real. The double-buffer mechanism at SimRuntime.cs:1228 alternates between _snapSlotA and _snapSlotB, and SnapshotList<T> exposes the raw backing T[] array by reference (SnapshotList.cs lines 9, 21, 39). The render thread in WorldRenderer._Draw() captures `_currSnap` and then iterates `snap.Dummies` (which is `new SnapshotList<DummyState>(DummiesBuf, DummiesCount)` — a direct referenc

### [ecs] RemoveDummy skips BedAssignee cleanup — assigned bed permanently blocked after colonist death
`Sim/SimRuntime.cs:7010`

RemoveDummy (called from KillColonist at line 5156) only releases BuildTarget and PathFollower before calling entity.DeleteEntity(). It never calls UnassignPawnBed, so the bed entity keeps its BedAssignee component with the dead pawn's ID. TryReserveBedForSleep at line 979 skips any bed whose entity HasComponent<BedAssignee>, so that bed is permanently inaccessible to all remaining colonists. Friflo does not fire component-removed events on entity deletion, so no automatic cleanup occurs. Additionally, any _bedReservations entry keyed to the dying pawn's bed is not removed.

> verify: The bug is confirmed real. KillColonist (line 5136) calls RemoveDummy (line 7010) without first calling UnassignPawnBed. RemoveDummy only cleans up BuildTarget and PathFollower, then calls ent.DeleteEntity() — no bed cleanup at all. UnassignPawnBed exists at line 879 and correctly removes BedAssignee from the bed entity, but it is never invoked in the death path. TryReserveBedForSleep at line 979 

### [ecs] RemoveDummy does not release Ur board seat reservations — dead pawn permanently occupies a seat
`Sim/SimRuntime.cs:7010`

When a pawn dies while seated at (or walking to) an Ur board, RemoveDummy deletes the entity without calling ReleaseUrSeat or removing AtRecreation / RecreationReservation. The _urBoardSeats and _urBoardPlayers dictionaries retain the stale seat entry, so TryPickFreePlayerSeat/TryReserveUrSeat sees the seat as occupied forever. ForgetUrBoard only fires on board decon. The AtRecreation component on the now-deleted entity is irrelevant since Friflo does not fire removed-events on deletion.

> verify: The bug is real and confirmed. `RemoveDummy` at /home/cowtools/struggle-game/Sim/SimRuntime.cs:7010-7027 handles only `BuildTarget` and `PathFollower` cleanup before calling `ent.DeleteEntity()`. It has no code to check for `AtRecreation` or `RecreationReservation` components and makes no call to `ReleaseUrSeat`. The death path (`KillColonist` at line 5136) calls `RemoveDummy(pawnId)` directly, so

### [ecs] Structural change (RemoveComponent) inside ForEachEntity in SetCheckmarkMode triggers StructuralChangeException
`Sim/SimRuntime.cs:575`

SetCheckmarkMode runs a ForEachEntity callback that calls AbortPawnJob(ent) when a job's work type is no longer allowed. AbortPawnJob unconditionally calls ent.RemoveComponent<BuildTarget>() at line 673 — a direct structural change on the queried entity while the ForEachEntity loop is active. Friflo 3.4 throws StructuralChangeException in this case (as explicitly documented in BlueprintClearanceSystem.cs line 87-88 for the same pattern). This crashes any time the player toggles checkmark mode while a colonist is mid-job.

> verify: The bug is confirmed real. SetCheckmarkMode at /home/cowtools/struggle-game/Sim/SimRuntime.cs:575 runs a ForEachEntity loop on _wandererQ and directly calls AbortPawnJob(ent) at line 582 inside the callback. AbortPawnJob unconditionally calls ent.RemoveComponent<BuildTarget>() at line 673 — a structural change on the iterated entity while the loop is active. When the pawn is carrying items, it add

### [ecs] Dying hauler's carried item entities are orphaned — items vanish from the world permanently
`Sim/SimRuntime.cs:5136`

KillColonist calls DropDownedItems then RemoveDummy. DropDownedItems only handles the Inventory component (equipped/held items); it does not touch the Carrying component. When a pawn is mid-haul at the time of death, the item entities in Carrying.Slots had their ItemPile component removed at pickup (OnHaulPickedUp, line 5519), so they exist in the ECS with no renderable component. RemoveDummy then DeleteEntity()s the pawn without calling DeliverCarrying, so those item entity IDs are never re-anchored as world ItemPiles. The picked-up items disappear permanently from the simulation.

> verify: The code confirms the bug exactly as described. KillColonist at line 5136 calls DropDownedItems (which only iterates Inventory.Equipped and Inventory.Items — no Carrying check) then RemoveDummy (which only releases BuildTarget jobs and discards PathFollower paths before DeleteEntity — no DeliverCarrying call). OnHaulPickedUp at line 5517 strips ItemPile from the carried entity at pickup time, so t

### [ecs] Stale Stove.ActiveCookEntityId after cook's death — stove permanently stuck on dead cook
`Sim/SimRuntime.cs:5136`

When a pawn dies while cooking (phase 1, standing at the stove), KillColonist -> RemoveDummy deletes the pawn entity without zeroing out the Stove.ActiveCookEntityId field on the bound stove. CookSystem.Step at line 112 gates progress on stove.ActiveCookEntityId == pawn.Id, so the stove never advances again (the cook entity is gone). CancelJob at line 3271 resets ActiveCookEntityId to 0, but RemoveDummy releases the BuildTarget job via Jobs.Release (not via CompleteJob/CancelJob), so the stove Cook job and ActiveCookEntityId are never cleaned up. The stove is stuck with a dangling cook ID until manually deconstructed.

> verify: The bug is real and verified by reading the code directly.

KillColonist (SimRuntime.cs:5136) calls RemoveDummy (line 7010), which does Jobs.Release(bt.JobId) — this only flips the cook job back to Open state in the job board; it does NOT call CancelJob.

CancelJob (SimRuntime.cs:3262-3271) is the sole code path that zeros stove.ActiveCookEntityId. Jobs.Release bypasses it entirely.

After the paw

### [logic] Idle-reload draft interaction double-consumes ammo and can reduce mag count
`Sim/World/DummyController.cs:336`

TopUpMagFromInventory (idle reload, undrafted path) consumes ammo from inventory and increments rc.MagCount (line 3244: `rc.MagCount += load`), then sets rc.Reloading=true with a timer. If the pawn is drafted before the timer elapses, the per-tick housekeeping at line 336 calls CompleteReload once the timer fires. CompleteReload does `rc.MagCount = load` (line 2996) — an absolute assignment, not an increment — and again subtracts `load` rounds from inventory. Result: inventory ammo is double-consumed, and if remaining inventory < MagazineSize the mag count is silently reduced below the value TopUpMagFromInventory left it at.

> verify: The bug is real. TopUpMagFromInventory (line 3227) immediately subtracts ammo from inventory (line 3242: stk.Count -= load) and increments MagCount (line 3244: rc.MagCount += load), then sets rc.Reloading=true with a timer. The per-tick housekeeping at line 336 — which runs unconditionally for ALL pawns with RangedCombat, before the if(drafted) branch at line 605 — calls CompleteReload when the ti

### [logic] Enemies that complete a mission without an Exfil step remain on the map permanently
`Sim/World/DummyController.cs:2219`

When StepMission advances MissionIndex past the last objective it returns EnemyGoalKind.None (value 0). The Execute switch at line 2173 has no case for None; it falls to default which calls ClearPath. The enemy then stands idle forever. No auto-Exfil or despawn is triggered. Combined with the staggered think timer, this means completed-mission enemies that were spawned without an Exfil objective at the end simply loiter on the map until the player notices.

> verify: The code confirms the claim exactly. At /home/cowtools/struggle-game/Sim/World/DummyController.cs line 2219-2220, StepMission returns EnemyGoalKind.None (value 0) when MissionIndex >= mission.Count. The Execute switch at line 2173-2198 has no case for None, so it falls to default: ClearPath(ref path) — the enemy stands idle. No other code path checks for brain.Goal == EnemyGoalKind.None to trigger

### [godot] IsInsideTree() guard defeats SizeChanged signal disconnect in _ExitTree — 14 panels
`Game/UI/ColonistBar.cs:67`

Every panel that subscribes to `GetTree().Root.SizeChanged += Reposition` in `_Ready` wraps the matching `-=` in `if (IsInsideTree())` inside `_ExitTree`. In Godot 4, `_ExitTree` fires after the node has already left the tree, so `IsInsideTree()` returns false and the disconnect never runs. The same pattern is copy-pasted across all 14 panel classes (ColonistBar, WorkTab, BillsPanel, CropInfoPanel, DebugBar, GrowZonePanel, HealthTabPanel, ItemInfoPanel, PawnInfoPanel, ScheduleTab, StockpilePanel, TileInfoPanel, Toolbar, TreeInfoPanel). Root (a Window) is never freed so it keeps stale connection entries for every panel that has exited the tree. The fix is to remove the `IsInsideTree()` guard — `_ExitTree` is the correct place for disconnect and `GetTree()` is still valid there in Godot 4.

> verify: The bug is confirmed real. All 14 panel classes in /home/cowtools/struggle-game/Game/UI/ share the identical pattern: they subscribe to `GetTree().Root.SizeChanged` in `_Ready` (unconditionally), and attempt to unsubscribe in `_ExitTree` behind `if (IsInsideTree())`. In Godot 4, `_ExitTree` fires after the node has already been removed from the scene tree, so `IsInsideTree()` returns false and the

### [godot] ColonistBar._Process allocates Dictionary, HashSet, and StringBuilder every frame
`Game/UI/ColonistBar.cs:75`

`_Process` runs at render rate (up to vsync, typically 60 Hz+). Each call unconditionally allocates: (1) `new Dictionary<int, DummyState>()` at line 75 to build byId; (2) `new System.Text.StringBuilder()` at line 89 for the signature; (3) `new List<DummyState>()` at line 95 on rebuild; (4) `new HashSet<int>(Host.SelectedDummyIds)` at line 102 for selection lookup. Additionally, every card gets two new `StyleBoxFlat`+`ScanlineStyleBox` objects on lines 124-125 via `CardBox()` and `FrameBox()` regardless of whether selection or mood changed. The Dictionary and HashSet should be instance-level reused scratch fields cleared at the top of _Process; the StyleBox allocations should be guarded by change detection.

> verify: The allocations are confirmed real by reading the code. At line 75, `new Dictionary<int, DummyState>()` is allocated unconditionally every frame with no class-level cached instance. At line 89, `new System.Text.StringBuilder()` is allocated unconditionally every frame. At line 102, `new HashSet<int>(Host.SelectedDummyIds)` is allocated unconditionally every frame. Lines 124-125 call `CardBox()` an

### [saveload] RemoveDummy leaks bed assignment and Ur board seat reservations on pawn removal
`Sim/SimRuntime.cs:7010`

RemoveDummy (called both directly and via KillColonist) calls ent.DeleteEntity() without invoking UnassignPawnBed or ReleaseUrSeat. The BedAssignee component on the bed entity still points to the dead pawn's id (which is now recycled), the pawn's AssignedBed pointer still holds the bed entity id, and any active Ur-board seat reservation in _urBoardSeats/_urBoardPlayers is never decremented. This means: a destroyed pawn's assigned bed is permanently locked out of the 'unowned unreserved' search in TryReserveBedForSleep (the BedAssignee check at line 979 still hits), and the Ur board shows wrong player/spectator counts indefinitely. Both are pure data-integrity bugs with no crash, just silent state corruption.

> verify: The bug is confirmed by reading the actual code. RemoveDummy at line 7010 only cleans up BuildTarget (job release) and PathFollower (path discard) before calling ent.DeleteEntity(). It never calls UnassignPawnBed or ReleaseUrSeat. The ItemSpatialIndex.cs comment explicitly documents that "Friflo does NOT fire OnComponentRemoved when an ENTITY is deleted", so DeleteEntity provides zero implicit cle

## MEDIUM

### [ecs] Non-ref GetComponent write silently discards TargetEntityId assignment
`Sim/World/DummyController.cs:2486`

Two sites in TickEngage call `entity.GetComponent<RangedCombat>()` (non-ref, returns a copy of the struct) and immediately write `.TargetEntityId = targetId` on the copy. Because RangedCombat is a value-type IComponent in Friflo, the write is to a temporary and the stored component is never updated. The enemy's RangedCombat.TargetEntityId therefore remains 0 or its previous value, so ExecuteRangedFire fires without a locked target. Lines 2486 and 2508 are both affected.

> verify: The non-ref GetComponent pattern at lines 2486 and 2508 is a real C# bug — writing to a value-type struct returned by value silently discards the write. The stored RangedCombat.TargetEntityId is never updated by those two lines.

However, the claim's stated consequence ("ExecuteRangedFire fires without a locked target") is overstated. ExecuteRangedFire and HandleRangedFire both receive the target 

### [logic] Drafted colonist cannot melee an enemy on the same tile — wastes a movement step each time
`Sim/World/DummyController.cs:840`

The melee adjacency predicate requires `ttile != here` (attacker and target on different tiles). When an enemy and the attacker share the same tile, adjacent is always false regardless of proximity. The code falls through to TryPickNeighbor which finds a walkable neighbor of the ATTACKER's tile (which is also the target's tile). The pawn paths one step away before it can swing. This also means TendedUntilTick refreshes happen only from adjacent tiles, but more importantly, same-tile melee can never be initiated — the attacker always backs off one tile first. The doctor-patient adjacency check at line 692 correctly allows same-tile with `ptile == here`; melee does not.

> verify: The code at line 840-841 of DummyController.cs confirms the bug exactly as described. The adjacency check is `bool adjacent = ttile != here && Math.Abs(ttile.X - here.X) <= 1 && Math.Abs(ttile.Y - here.Y) <= 1;` — the `ttile != here` clause explicitly excludes same-tile. When attacker and target share a tile, adjacent=false, the swing is skipped, and TryPickNeighbor is called with the target's til

### [perf] Per-tick HashSet allocations in RebuildStationaryAvoid (every 10 ticks) and RebuildColonistLosTiles (every 60 ticks)
`Sim/SimRuntime.cs:4513`

RebuildStationaryAvoid allocates a fresh HashSet<TilePos> every 10 ticks (~6/s at 60Hz). RebuildColonistLosTiles at line 4541 does the same every 60 ticks. These sets can contain hundreds of tiles (ColonistLosRadius=18 means up to ~1017 tiles per colonist). Both methods create a brand-new heap object then atomically replace the previous reference. At scale these become significant GC pressure. The sets should be pre-allocated as fields and reused across calls (swap double-buffer or clear-and-refill with a field-level HashSet).

> verify: The code at the cited lines is exactly as described. RebuildStationaryAvoid at line 4513 allocates `new HashSet<TilePos>()` every StationaryAvoidRebuildInterval=10 ticks, and RebuildColonistLosTiles at line 4541 does the same every ColonistLosRebuildInterval=60 ticks. PathService.SetDefaultAvoid (PathService.cs line 86-89) simply stores the reference as `_defaultAvoid`, and _colonistLosTiles recei

### [perf] Per-pawn per-snapshot heap allocations for inventory/injury arrays inside BuildSnapshot
`Sim/SimRuntime.cs:1336`

Inside the per-pawn ForEachEntity loop in BuildSnapshot (which runs every tick), each pawn with a Carrying component allocates a new CarriedItemState[], each pawn with an Inventory allocates new EquippedSlotState[] and HeldStackState[], and each pawn with injuries allocates a new InjuryState[]. With N armed/carrying pawns this is 3–4 small array allocs per pawn per tick. These should be pooled into the per-pawn slot in snap.DummiesBuf or into a pool parallel to PawnWorkPriPool (which the existing code already uses for work-tab arrays).

> verify: The allocations are exactly as described. At /home/cowtools/struggle-game/Sim/SimRuntime.cs lines 1336, 1359, 1373, and 1400, inside the per-pawn ForEachEntity loop in BuildSnapshot (called every tick from SimHost.cs line 396), fresh arrays are allocated with `new CarriedItemState[c.Slots.Count]`, `new EquippedSlotState[inv.Equipped.Count]`, `new HeldStackState[inv.Items.Count]`, and `new InjurySt

### [perf] TryFindBestHaulDest two-argument overload allocates a new Dictionary<TilePos,int> + full ItemPile ECS scan on every call
`Sim/SimRuntime.cs:3909`

The two-argument overload of TryFindBestHaulDest (called from BlueprintHaulSystem and any path that doesn't pass a pre-built index) allocates a fresh Dictionary<TilePos,int> and runs a full ECS scan over every ItemPile to populate it, then throws the dictionary away. HaulSystem.Step already has the right pattern (pre-built _stackAt). Any caller routing through this overload pays an O(items) scan + dictionary alloc per call. Callers should be plumbed to pass the pre-built index, or the two-argument overload should maintain a cached scratch dictionary.

> verify: The claim is real and accurate. At /home/cowtools/struggle-game/Sim/SimRuntime.cs:3906-3916, the three-argument overload (source, def, countToMove, out dest, out stockpileId) allocates `new Dictionary<TilePos, int>()` and runs a full ForEachEntity ECS scan over every ItemPile on every call before delegating to the four-argument overload that accepts a pre-built index. HaulSystem.Step (HaulSystem.c

### [godot] DraftActionBar._Process allocates new StyleBoxFlat objects every frame for every tile
`Game/UI/DraftActionBar.cs:256`

Every frame that a pawn is selected, `_Process` calls `SetTileActive` for up to 5 buttons (lines 230, 267, 268, 276, 277). Each `SetTileActive` call invokes `ScanTile(TileBg, border)` twice, which calls `MakeBox(...)` returning a `new StyleBoxFlat` each time and `UiTheme.Scan(...)` returning a `new ScanlineStyleBox`. Additionally, line 256 unconditionally calls `_magBar.AddThemeStyleboxOverride("fill", MakeBox(AmmoColor(...), ...))` — a new `StyleBoxFlat` every frame regardless of whether the loaded ammo changed. Each `AddThemeStyleboxOverride` call also invalidates the control's theme cache and queues a redraw. The fix is to track the last active/border state per tile and skip the override when nothing changed, and to track `_lastAmmoPath` to guard the mag bar override.

> verify: The code at the cited location confirms the claim. `_Process` runs every frame with zero change-detection. `SetTileActive` (called up to 5 times per frame) invokes `ScanTile` twice per call; each `ScanTile` calls `MakeBox` (returns `new StyleBoxFlat`) and `UiTheme.Scan` (returns `new ScanlineStyleBox`). Line 256 unconditionally calls `MakeBox(AmmoColor(p.LoadedAmmoPath), ...)` — a `new StyleBoxFla

### [godot] SimHost.IsPlayerWall reads List<TilePos> from render thread without _mapLock
`Game/SimHost.cs:248`

`IsPlayerWall` exposes `_sim.PlayerWalls` which is the live `List<TilePos>` field `_playerWalls`. The sim thread mutates `_playerWalls` inside `lock(_mapLock)` blocks (at SimRuntime.cs lines 2094, 2697, 2746, 3024). The render/main thread calls `IsPlayerWall` from `WallInfoPanel.Render`, `Bootstrap._UnhandledInput`, and the info panel `_Process` tick — all without taking `_mapLock`. A concurrent `Add`/`Remove` while `Contains` iterates the list can cause index-out-of-range exceptions or silent wrong reads. The fix is to either take `lock(_swapLock)` in `IsPlayerWall` (matching the other locked accessors like `CanPlaceBed`) or add the snapshot's `SelectedWallTiles` field to carry the player-wall flags instead.

> verify: The race is real and unguarded. `SimHost.IsPlayerWall` at line 248 calls `_sim.PlayerWalls.Contains(tile)` where `_sim` is the live `SimRuntime` and `PlayerWalls` (SimRuntime.cs:3338) returns the raw `_playerWalls` List<TilePos> field directly — no snapshot, no copy. The sim thread mutates this list under `lock(_mapLock)` at SimRuntime.cs lines 2094, 2697, 2746, and 3024. Three callers on the rend

### [saveload] KillColonist names corpse with pawnId but snapshot reads ColonistNames.For(ent.Id) — the two name sources can diverge
`Sim/SimRuntime.cs:5155`

KillColonist creates the Corpse component with Name = $"Colonist {pawnId}" (a plain integer). But the live DummyState for colonists uses ColonistNames.For(ent.Id) (line 1583), which produces a stable two-word name like 'Vael Brokk'. The corpse's MakeDeadDummy reads corpse.Name (line 1622), so the dead colonist's portrait and info panel will show e.g. 'Colonist 14' while the live colonists all showed 'Vael Brokk'. A future resurrection system or any UI matching live-vs-dead by display name would mismatch.

> verify: The bug is confirmed. At /home/cowtools/struggle-game/Sim/SimRuntime.cs:5155, KillColonist stores `Name = $"Colonist {pawnId}"` in the Corpse component. The live dummy snapshot (line 1583) uses `ColonistNames.For(ent.Id)` which produces a stable two-word name like "Vael Brokk" derived deterministically from the entity ID. The dead dummy snapshot (line 1622) reads `corpse.Name` directly, so the dea

### [saveload] Stale BedReservation leak: _bedReservations is never pruned for non-sleeping pawns whose reservations were never explicitly released
`Sim/SimRuntime.cs:960`

The comment at line 961 explicitly documents a known stale-pointer scenario: 'Stale pointer: bed was destroyed without firing ForgetBed (shouldn't happen — ForgetBed runs on decon). Fall through to nearest-bed search; the dangling AssignedBed will be overwritten or cleaned up on next ForgetBed.' Similarly, _bedReservations maps bed entity id → pawn entity id. ReleaseBedReservation is the only removal path besides ForgetBed. If a pawn that has an in-flight bed reservation is removed via RemoveDummy (harness, debug), the _bedReservations entry is never removed. On the next TryReserveBedForSleep scan, any other pawn trying to claim that bed hits the 'resOwner != pawn.Id' guard (line 983) and skips it — permanently locking a valid bed.

> verify: The bug is confirmed real. RemoveDummy at line 7010 cleans up BuildTarget and PathFollower but never calls ReleaseBedReservation. A pawn walking toward a bed (reservation held in _bedReservations, but Sleeping component not yet added) that is deleted via RemoveDummy (including the death path at line 5156 which calls RemoveDummy) leaves a stale entry in _bedReservations. The scan in TryReserveBedFo

## LOW

### [threading] Host.Map.GetWall() / Walkable() read TileMap._walls[] from render thread without lock
`Game/UI/WallInfoPanel.cs:73`

WallInfoPanel and Selector.cs call Host.Map.GetWall() and Host.Map.Walkable() directly on the Godot main thread. These read TileMap._walls[], a plain byte[] that the sim thread writes via Map.SetWall() inside lock(_mapLock). The callers hold no lock of their own. Concurrent byte[] element writes vs reads are individually atomic on x86/x64 for naturally-aligned scalar types, but no memory barrier or volatile annotation protects visibility: the JIT/CPU can reorder or cache-stale the read. More dangerously, the chunk-dirty-flag path in TileMap reads and writes _walls[] without any fence outside the lock, so a render-thread read racing a sim-thread write has no guaranteed ordering.

> verify: The race is real but the severity is overstated. Confirmed facts:

1. `SimHost.Map` at /home/cowtools/struggle-game/Game/SimHost.cs:38 returns `_sim.Map` — the live mutable `TileMap` instance, not a snapshot copy.

2. `WallInfoPanel.Render()` (line 73, 88, 92) and `Selector.cs:1034` both call `Host.Map.GetWall()` directly on the Godot main thread, with no lock held.

3. `SimRuntime` wraps `Map.Set

### [threading] WorkerLoop stores result into _ready after checking _pending with a TOCTOU gap
`Sim/Pathfinding/PathService.cs:123`

WorkerLoop checks _pending.ContainsKey(item.Id) and then separately stores into _ready[item.Id]. Between the ContainsKey check and the _ready assignment, the sim thread can call Discard(id) which removes the id from both _pending and _ready. If Discard runs in that window, _ready gets a stale result that will never be consumed (the id is gone from _pending, so TryConsume will not find it either). This wastes one ConcurrentDictionary slot until the next Discard/GC but does not corrupt correctness — the leaked entry is small and inert. The fix is to do the check-and-assign atomically with TryAdd or to accept the leak as a known minor waste.

> verify: The race exists exactly as described. At line 117, the worker does an early-bail ContainsKey check, then runs A*, then at line 123 does a second ContainsKey check before storing into _ready. These two operations at line 123 are not atomic. Discard() (lines 143-145) removes from _pending first, then from _ready. If Discard runs after the ContainsKey check at line 123 returns true but before _ready[

### [threading] RoomIdAt / _roomTiles array read by render thread comment is misleading — no actual lock taken
`Sim/SimRuntime.cs:5949`

The comment at RoomIdAt says 'caller is the sim thread or the render thread under _mapLock semantics (snapshot publishing guarantees stability between recompute and snapshot read)'. However CopyRoomTilesForRender() correctly clones _roomTiles under lock(_mapLock), while RoomIdAt() itself takes NO lock. SimHost exposes RoomIdAt() / TileTempC() / RoomTempC() as direct unlocked delegates. If ever called from the Godot main thread (the methods are public and wired into SimHost), they race with DoRecomputeRooms() which allocates a brand-new _roomTiles = new int[n] array and immediately writes into it on the sim thread — the render thread could be mid-read of the old reference while the sim has already replaced it with a new array. Currently no render-side code calls these methods, but the public API with misleading locking comment is a maintenance trap.

> verify: The claim is accurate. DoRecomputeRooms() at SimRuntime.cs:6065 replaces _roomTiles with a new array reference without holding _mapLock. RoomIdAt() at line 5949-5955 reads _roomTiles directly with no lock. The comment at lines 5944-5948 falsely claims _mapLock semantics protect this read — but _mapLock is never taken inside DoRecomputeRooms(). CopyRoomTilesForRender() at line 6974 correctly takes 

### [logic] Hold mission step never advances when enemy arrives at tick 0 due to PhaseStartTick sentinel collision
`Sim/World/DummyController.cs:2233`

PhaseStartTick = 0 is used as the 'not yet started' sentinel for a Hold objective. If _tick == 0 when the enemy first arrives at the Hold tile, brain.PhaseStartTick is assigned 0 (line 2233). On every subsequent think tick the guard `if (brain.PhaseStartTick == 0)` is still true, so the timer is reset to the new _tick instead of elapsing. The hold duration never expires and the enemy is stuck in Hold indefinitely. Tick 0 is reachable in tests and theoretically at cold start before the first Tick++ fires.

> verify: The sentinel collision is real code. PhaseStartTick = 0 is used both as "not started" and as a valid tick value. _tick = 0 IS passed to DummyController.Step on the very first SimRuntime.Step call (line 432 passes Tick before line 496 increments it). NextThinkTick defaults to 0, so the think fires on tick 0. If an enemy arrives at a Hold tile on tick 0, brain.PhaseStartTick is written with 0, which

### [logic] HealthSystem.Advance heals stabilized wounds at the same base rate as untreated wounds
`Sim/World/HealthSystem.cs:120`

The healing rate formula only grants the fast-heal bonus for Tended wounds. Stabilized wounds (Tended=false, Stabilized=true) use the base HealPerSecHp rate — identical to completely untreated wounds. From the design described in comments: stabilize 'quick patch — bleed cut ~75%, nothing else'. The 'nothing else' likely refers to no pain reduction and no heal speed bonus, but the result is that stabilizing a wound provides exactly the same healing rate as leaving it untreated, making stabilize purely a bleed-reduction tool with no healing benefit over no-treatment.

> verify: Line 120 of HealthSystem.cs confirms the claim exactly: `float rate = HealPerSecHp * (inj.Tended ? 1f + TendHealQualityBonus * inj.TendQuality : 1f);` — the ternary only checks `inj.Tended` and never references `inj.Stabilized`. Both stabilized and untreated wounds fall into the else branch and use `rate = HealPerSecHp * 1f`. The `BleedOf` method (lines 130–136) does check `inj.Stabilized` and app

### [logic] _lastJobSeek cache leaks entries for dead/removed pawn entity IDs
`Sim/World/DummyController.cs:1408`

The _lastJobSeek dictionary maps pawn entity IDs to (version, tick) throttle state. Entries are added on every TryClaimJob call but never removed when a pawn entity is deleted (death, decon, debug removal). Over a long session with many pawn deaths and new spawns, the dictionary accumulates dead-pawn entries that are never evicted. Friflo ECS can reuse entity IDs, so a new pawn spawned with the same ID as a dead one will inherit the stale throttle state from the previous owner, potentially delaying its first job claim by up to JobReseekInterval=10 ticks.

> verify: All three parts of the claim are verified by the code and Friflo ECS behavior:

1. **No eviction on death**: `_lastJobSeek` at line 125 is written at line 1408 on every `TryClaimJob` call and is never cleaned up. `RemoveDummy` / `KillColonist` (SimRuntime.cs:7010, 5136) call `ent.DeleteEntity()` but never touch `_lastJobSeek`.

2. **Friflo ECS does recycle integer entity IDs**: The project uses `F

### [logic] Enemy perception LoS uses tile-integer positions while colonist targets are stored as floats (effective lean positions)
`Sim/World/DummyController.cs:2336`

PerceiveNearestColonist stores the colonist's effective (lean-adjusted) float position in _colonistTargets, but the LoS gate casts those floats to int for the LosClear call. A colonist leaning around a wall corner may have a float effective position that crosses a tile boundary, so the LoS check fires against the wrong tile. In practice the lean fraction (LeanPeekFraction) is small enough that integer truncation usually lands in the right tile, but a pawn positioned at exactly a tile edge while leaning could be incorrectly perceived or missed.

> verify: The bug is confirmed. At line 2336, PerceiveNearestColonist calls LosClear(here.X, here.Y, (int)t.X, (int)t.Y) where t.X/t.Y are the float effective positions from EffectivePos. With LeanPeekFraction=0.6f, a pawn leaning from body tile 5 toward peek tile 6 has effective X=5.6, which truncates to 5 — back to the body tile. The comment at lines 209-211 explicitly states the intent is to use the peek

### [perf] Per-snapshot lambda allocations for aimIsWall and aimIsSandbag closures inside BuildSnapshot
`Sim/SimRuntime.cs:1282`

BuildSnapshot is called every sim tick (line 396 in SimHost.cs). It allocates two new Func<int,int,bool> delegate objects per call at lines 1282-1285. The aimIsWall lambda captures `this` (Map field) and aimIsSandbag captures `_sandbagMap`. Because the lambda bodies contain a captured field reference, the compiler cannot cache them as static delegates. Over 60 ticks/s these produce ~120 heap allocations per second that are immediately eligible for GC. Both should be converted to pre-allocated cached field delegates.

> verify: The code at SimRuntime.cs:1282-1285 exists exactly as described and BuildSnapshot is called every sim tick (SimHost.cs:396). The aimIsWall lambda captures `this` via the `Map` instance property (TileMap Map { get; } at line 20), and aimIsSandbag captures `_sandbagMap` (a readonly instance field, line 190). The C# compiler cannot cache either as a static delegate since both have instance captures. 

### [perf] new HashSet<int>(selectedDummyIds) allocation on every BuildSnapshot call when pawns are selected
`Sim/SimRuntime.cs:1250`

BuildSnapshot constructs a new HashSet<int> from the selectedDummyIds array on every call when at least one pawn is selected. This is the common runtime state (player always has at least one pawn selected). The selection array rarely changes. The HashSet could be cached by reference-identity of the input array (same discipline the renderer already uses for SelectedTreeIds etc.) to avoid rebuilding it every tick.

> verify: The allocation is real. At /home/cowtools/struggle-game/Sim/SimRuntime.cs:1250, `new HashSet<int>(selectedDummyIds)` is constructed unconditionally every time BuildSnapshot is called with a non-empty selection array. In the normal (unpaused) sim loop path in SimHost.cs (line 396), BuildSnapshot is called on every sim tick with no needPublish guard — the needPublish guard only exists in the _paused

### [perf] StepFires allocates new List<int> per unique pawn tile on every fire tick
`Sim/SimRuntime.cs:5208`

Every time StepFires runs (gated by FireStepEveryTicks but still periodic), it builds _firePawnsByTile by allocating a new List<int>() for each distinct tile where a pawn stands. The dictionary is cleared at the top of StepFires but the inner lists are discarded and reallocated next call rather than being cleared and reused. In a colony with many pawns during a fire event this generates O(unique-occupied-tiles) List<int> allocations per fire tick.

> verify: The code at /home/cowtools/struggle-game/Sim/SimRuntime.cs:225 declares `_firePawnsByTile` as a reused Dictionary field. However, at line 5204 it calls `.Clear()` on the dictionary, which discards all stored List<int> references without pooling them. Then at line 5208, for each unique tile a `new List<int>()` is unconditionally allocated and stored. The old lists are abandoned to GC. So the alloca

### [perf] SimWatcher.Report calls _recent.ToArray() + _recent.RemoveAt(0) on every anomaly, both O(n)
`Sim/Diagnostics/SimWatcher.cs:178`

Every call to Report (stuck pawn, brain-dead pawn, rescued pawn) calls _recent.RemoveAt(0) which is O(MaxRecent=64) due to List<T> shifting, and then _recent.ToArray() which allocates a new SimAnomaly[64] array. When anomalies fire repeatedly (e.g. multiple stuck pawns), this is called every SampleEveryTicks (30 ticks). Both the shift and the ToArray should be replaced with a fixed-size circular buffer; the published snapshot could reuse a single pre-allocated array.

> verify: The code at lines 178-179 of /home/cowtools/struggle-game/Sim/Diagnostics/SimWatcher.cs exactly matches the claim: _recent.RemoveAt(0) is O(n) due to List<T> shifting, and _recent.ToArray() allocates a new array on every Report call. However, the real-world impact is much lower than "medium" implies. MaxRecent=64 is tiny — the shift moves at most 63 elements, and the array allocation is 64 * ~24 b

### [perf] _lastJobSeek dictionary in DummyController never evicts dead-pawn entries
`Sim/World/DummyController.cs:125`

DummyController._lastJobSeek maps pawn entity IDs to (Version, Tick) pairs as a job-seek throttle. Entries are written every time TryClaimJob runs (up to once per idle pawn per tick) but are never removed when a pawn dies or is despawned. Over a long session with pawn churn (spawns + kills) this dictionary accumulates stale entries for all entity IDs that ever existed. SimWatcher has the same pattern but actively prunes dead entries; _lastJobSeek has no corresponding cleanup.

> verify: The claim is accurate. _lastJobSeek at line 125 of DummyController.cs has only three references in the file: declaration, a read at line 1402, and a write at line 1408. There is no Remove/Clear/eviction anywhere in the class. The Step method only iterates over live Wanderer pawns, so dead pawn entries accumulate indefinitely. SimWatcher.cs (lines 168-170) has exactly the pruning pattern described 

### [godot] WorldRenderer._Process calls QueueRedraw() unconditionally driving _Draw every frame even when nothing changed
`Game/Render/WorldRenderer.cs:261`

`_Process` calls `QueueRedraw()` every frame without checking whether any visual state (snapshot, selection, fire animation frame) has actually changed. `_Draw` is a large method that iterates all tiles, dummies, blueprints, and trees. While this is intentional for animated content (selection pulse, fire sprite, lighting interpolation), the unconditional call means the full draw path runs even when the game is paused and nothing on screen has moved. Paused gameplay with no fire tiles or selection animations could be optimized by tracking a dirty flag. This is a design decision, but it contributes to steady GPU load even on a static scene.

> verify: The code at WorldRenderer.cs:258-263 exactly matches the claim — `_Process` calls `QueueRedraw()` every frame unconditionally. `_Draw` (line 265+) does iterate tiles, dummies, blueprints, and trees on every frame. There is no dirty flag or early-exit guard at the top of `_Draw` to skip the full traversal when nothing changed.

However, partial mitigation exists: inside `_Draw`, expensive overlay r

### [godot] VisualLighting.Tick() called on every render frame instead of on snapshot change
`Game/Render/WorldRenderer.cs:262`

`WorldRenderer._Process` calls `_visualLighting?.Tick()` every frame. `VisualLighting.Tick` in turn calls `Host.LatestSnapshot` (a Volatile read) and `UpdateHalos(snap)` on every new snapshot reference, and `RebuildLightTexture()` on every `LightVersion` bump. `UpdateHalos` has internal deduplication (reference + tick check at line 104), so it's not pathological, but it still executes `Host.LatestSnapshot`, a reference compare, and the halo-seen set logic every single render frame. Since Tick() is already internally version-gated, this is safe but wastes a small amount of work each frame on the inner null/version checks when nothing has changed.

> verify: The code at WorldRenderer.cs:262 and VisualLighting.cs:92-110 confirms the described behavior exactly. `_Process` calls `_visualLighting?.Tick()` every render frame. `Tick()` always executes a volatile `Host.LatestSnapshot` property read, a `LightVersion` integer compare, a `ReferenceEquals`, and a `snap.Tick` compare — regardless of whether anything changed. The internal version gates (lines 98 a

### [saveload] CookSystem.HasIngredientsOnMap counts reserved (HaulReserved) item stacks as available, allowing a bill to start when ingredients are already in-flight to another destination
`Sim/World/CookSystem.cs:195`

HasIngredientsOnMap builds _pathTotals by scanning every ItemPile without filtering out stacks that carry a HaulReserved component (already claimed by an in-flight haul job). If 5 carrots exist but a hauler has already reserved and picked them up (removing the ItemPile component), they would no longer show; but if the haul is pending pickup (HaulReserved is set, ItemPile still exists), they still count. This can cause a Cook job to be posted and a pawn bound to it even though the haul system has already promised those carrots to a blueprint deposit, leading to ingredient contention. The haul system itself has the same issue in reverse: it checks reservation via HaulReserved but CookSystem does not.

> verify: The claim is real but the severity and impact description are partially overstated. Confirmed: EnsureTotals at line 47 scans all ItemPile entities via store.Query<ItemPile>() with no HaulReserved filter, so HasIngredientsOnMap does count reserved stacks. This can cause a Cook job to be posted when all matching items are already reserved by in-flight haul jobs.

However, the actual harm is less sev

### [saveload] Bill.StockpileEntityId is stored as an ECS entity id but is never validated at cook completion — stale id silently falls back to DropAtWorkbench without warning
`Sim/World/Components.cs:1101`

Bill holds StockpileEntityId (an ECS entity id) for the SpecificStockpile output destination mode. CookSystem.Step finishes by calling _sim.SpawnItemPile with no routing to the target stockpile (line 141 — it just drops at f.OutputTile). The SpecificStockpile/AnyStockpile dispatch appears to be a stub not yet wired in CookSystem. If a player sets OutputDest = SpecificStockpile and then the target stockpile is deleted, the stale entity id is never checked and CookSystem silently drops the meal at the workbench regardless — this is the expected fallback but there is no notification or BillState update to indicate the target was lost.

> verify: The claim is accurate. BillOutputDest is a real enum with DropAtWorkbench=0, SpecificStockpile=1, AnyStockpile=2 (Components.cs lines 1039-1046), and Bill.StockpileEntityId is a real field (line 1101). However, CookSystem.Step never reads bill.OutputDest anywhere — the CookFinish struct only captures OutputTile/OutputItemPath/OutputCount, not OutputDest or StockpileEntityId, and the completion loo

### [saveload] Mood value in DummyState is a compile-time hash stub — never read from any component, so it can never be saved or restored
`Sim/SimRuntime.cs:1582`

The Mood field in DummyState is computed inline as a deterministic hash of the entity id (line 1582): '0.25f + 0.75f * (((ent.Id * 2654435761u) % 1000u) / 1000f)'. It is not stored in any ECS component. This means if entity ids are ever remapped (e.g. after a future save/load that reorders entities), every colonist's mood portrait color will change silently. More directly, any future mood system that does store mood in a component will be invisible to the renderer until that line is updated — easy to miss because the UI already shows 'working' mood colors from the hash stub.

> verify: The claim is factually accurate. At /home/cowtools/struggle-game/Sim/SimRuntime.cs:1582, the Mood field in DummyState is populated with a deterministic hash of ent.Id (0.25f + 0.75f * (((ent.Id * 2654435761u) % 1000u) / 1000f)) — confirmed by reading the code directly. There is no ECS Mood component anywhere in the codebase (grep for struct/class/record Mood and MoodComponent returned nothing). Th
